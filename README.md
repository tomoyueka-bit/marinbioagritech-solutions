# MarinBio AgritechSolutions website

Production multi-page website for MarinBio AgritechSolutions Ltd.

Vercel installs the Node `tar` package, runs `extract.mjs`, expands the validated site package into `public/`, and deploys that static output.
