{
  "name": "marinbioagritech-solutions",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && tar -xzf site.tar.gz -C dist && rm -f dist/vercel.json"
  }
}
